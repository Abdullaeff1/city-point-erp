# Production (copy .env.example to .env first)
#   docker compose -f bin/prod/docker-compose.yml --env-file bin/prod/.env up --build
#
# Security docs:
#   docs/security/database-isolation.md
#   docs/security/resident-portal-access.md
#   docs/security/postgres-roles.md
#   docs/security/network-checklist.md
#
# Postgres: create cp_migrator / cp_app / cp_readonly (postgres-roles.md),
# run migrate as cp_migrator, then set POSTGRES_USER=cp_app for the web service.
#
# Invite a resident portal user (no public signup):
#   docker compose exec web python manage.py invite_portal_user \
#     --email=office@tenant.az --company=asbc --first-name=Office --last-name=Manager \
#     --base-url=https://portal.citypoint.az --send-email
